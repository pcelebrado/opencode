# Sample Repos for Jules

- `todo-api-ts` - TypeScript API validation and filtering tasks
- `py-backend-hardening` - Python backend validation and duplicate handling
- `react-kanban-widget` - UI state/persistence/move logic tasks

Each repo is intentionally incomplete so Jules can implement scoped fixes.
